@using VT.Model.ViewModel;
@model ItemCheckBox

<label>
    @Html.HiddenFor(x => x.Id)
    @Html.CheckBoxFor(x => x.Checked, new { style = "width: 20px;height: 20px;" })
    <span style="display:inline-block; margin-right:15px;">
        @Model.Name
    </span>
</label>